// Раздел 1: ВВЕДЕНИЕ — ГОСТ 19.201-78, п. 2.1
#import "../templates/gost19.typ": note, glos-entry

// =============================================================
= ВВЕДЕНИЕ
// =============================================================

== Наименование темы разработки

Наименование темы разработки -- *«Очередь сообщений на языке Go с гарантиями доставки и горизонтальным масштабированием»*.

== Наименование программы

Наименование программы -- *«Очередь сообщений на языке Go с гарантиями доставки и горизонтальным масштабированием»* (далее -- «Брокер», «Система», «BunnyMQ»).

Наименование на английском языке -- *«Go message queue with delivery guarantees and horizontal scaling»*.

Краткое наименование: *BunnyMQ*.

Обозначение программного документа по ГОСТ~19.103-77: *RU.17701729.02-06 ТЗ 01–1* (числовой код вида документа по ГОСТ~19.101-77: *05*).

Вид программного изделия по ГОСТ~19.101-77, §2.2: *комплекс программ* -- совокупность взаимодействующих программных компонентов, объединённых в единую систему: серверный процесс брокера, клиентская библиотека и утилита командной строки.

== Состав комплекса программ

Комплекс BunnyMQ включает три компонента:

+ `bunnymq` (`cmd/bunnymq`) -- серверный процесс брокера; запускает gRPC-серверы, узел Raft, координаторы и HTTP-эндпоинт метрик;
+ `bunnymq-cli` (`cmd/bunnymq-cli`) -- утилита командной строки для управления кластером и тестирования: команды `produce`, `consume`, `topic`, `cluster`;
+ `pkg/client` -- публичная Go-библиотека с типами `Producer`, `Consumer`, `AdminClient` и автоматическим поиском лидера.

== Краткая характеристика области применения

BunnyMQ предназначен для инфраструктуры микросервисных приложений, где требуется надёжная асинхронная передача сообщений с гарантиями доставки и возможностью горизонтального масштабирования.

Типичные сценарии:

- передача событий между сервисами с гарантией «хотя бы однократной» доставки (acks=all);
- сбор телеметрии и логов от большого числа продюсеров (acks=0);
- организация нескольких независимых потоков данных через механизм топиков и партиций;
- параллельная обработка данными несколькими консьюмерами через группы с автоматическим перераспределением партиций.

Категории пользователей:

- *Разработчики приложений-продюсеров* -- подключаются через `pkg/client` или gRPC напрямую, отправляют сообщения в топики с выбором уровня подтверждения;
- *Разработчики приложений-консьюмеров* -- читают из партиций по смещению, при необходимости используют группы консьюмеров;
- *Администраторы кластера* -- управляют топологией через `bunnymq-cli` или Management API: создают и удаляют топики, меняют настройки хранения, просматривают состояние кластера.
