// Раздел 3: НАЗНАЧЕНИЕ РАЗРАБОТКИ (Aparkin) - ГОСТ 19.201-78, п. 2.3

// =============================================================
= НАЗНАЧЕНИЕ РАЗРАБОТКИ
// =============================================================

== Функциональное назначение программы

Серверная часть 1 платформы CSTATI.events (events-service, payment-service, qr-service) реализует автоматизацию ключевых операций управления жизненным циклом мероприятий и обработки платежей:

+ *Управление мероприятиями* --- создание, редактирование и удаление событий; управление статусами мероприятий; публикация мероприятий в открытом доступе; установка параметров события (дата, время, место, описание, ценовые категории).

+ *Управление волнами регистрации* --- определение нескольких волн регистрации для одного события; автоматическое управление статусами волн (запланирована, активна, приостановлена, завершена); управление сроками открытия и закрытия волн.

+ *Управление билетными категориями* --- определение нескольких типов билетов с разными ценами; настройка количества билетов в категории; атомарное резервирование мест; управление статусами билетов (активен, скрыт, распродан, архивирован).

+ *Управление промокодами* --- создание кодов скидок с процентной скидкой; установка лимитов использования и сроков действия; атомарная активация промокодов с ограничением по частоте запросов (максимум 5 активаций/мин с одного IP).

+ *Регистрация участников* --- приём заявок на участие с проверкой наличия свободных мест; применение промокодов и расчёт финальной стоимости; отправка заявки в очередь обработки Kafka; управление статусами регистрации (ожидание оплаты, подтверждена, отменена, истекла, неудачная, оплачена, пройден контроль).

+ *Обработка онлайн-платежей* --- интеграция с платёжным шлюзом ЮKassa; создание платёжных ссылок с типом подтверждения `redirect`; обработка вебхуков о статусе платежей; обеспечение идемпотентности операций через ключи идемпотентности; поддержка платных и бесплатных билетов (для бесплатных обращение к ЮKassa не производится).

+ *Генерация QR-кодов* --- генерирование PNG-изображений QR-кодов размером 256×256 пиксельной, кодирующих уникальный идентификатор регистрации; загрузка в объектное хранилище MinIO по протоколу S3; публикация публичных URL для доставки участникам.

+ *Контроль прохода на мероприятие* --- верификация участников по QR-коду электронного билета; получение информации о регистрации на контрольном пункте; предотвращение повторного входа.

+ *Асинхронная обработка сообщений* --- взаимодействие с Kafka для коммуникации с другими сервисами: публикация событий в топики `registrations`, `payments`, `qr-generation`; потребление сообщений из топика `ticket-status-updates` для обновления статуса билетов.

== Эксплуатационное назначение программы

=== Архитектура развёртывания

Микросервисы events-service, payment-service, qr-service развёртываются в контейнеризированной среде Docker:

+ *Docker-контейнеризация*: каждый микросервис упаковывается в отдельный Docker-контейнер с указанием базового образа (go:1.24-alpine); управление контейнерами через Docker Compose в среде разработки;

+ *Постоянное хранилище*:
  - events-service: PostgreSQL 16 (БД `events_db`);
  - payment-service: PostgreSQL 16 (БД `payment_db`);
  - qr-service: PostgreSQL 16 (БД `qr_db`);
  - MinIO (S3-совместимое хранилище): QR-коды и прочие артефакты;
  - Redis 7: кэш Idempotency-Key для регистраций (TTL 24 ч).

+ *Очереди сообщений*: Apache Kafka для асинхронной коммуникации между events-service, payment-service, notifier, tg-bot:
  - топик `registrations`: события о новых регистрациях;
  - топик `payments`: события о статусах платежей;
  - топик `qr-generation`: события о необходимости генерации QR-кода;
  - топик `ticket-status-updates`: события о статусе билетов;
  - топик `registrations-dlq`: Dead Letter Queue для неудачных сообщений.

+ *Сетевая архитектура*: все микросервисы находятся в одной Docker-сети; публичный доступ через Nginx (обратный прокси на порту 443 HTTPS) и gRPC-эндпоинты для внутренней коммуникации с auth-manager (порт 50051).

=== Требования к окружению

+ *Системные требования*: Linux-сервер (Ubuntu 20.04+) с минимум 4 ГБ оперативной памяти и 50 ГБ дискового пространства;

+ *Программное обеспечение*: Docker 20+, Docker Compose 2.0+;

+ *Внутренние зависимости*:
  - PostgreSQL 16 (три отдельные БД);
  - Apache Kafka 3.0+ с минимум 3 топиками;
  - Redis 7;
  - MinIO (для хранения QR-кодов);
  - auth-manager (gRPC на порту 50051) для проверки JWT-токенов;
  - notifier (Kafka-потребитель) для доставки уведомлений.

+ *Конфигурация окружения*: переменные окружения для каждого сервиса:
  - `DATABASE_URL` для подключения к PostgreSQL;
  - `KAFKA_BROKERS` для подключения к Kafka;
  - `REDIS_URL` для подключения к Redis;
  - `S3_ENDPOINT`, `S3_ACCESS_KEY`, `S3_SECRET_KEY` для MinIO;
  - `YUKASSA_API_KEY`, `YUKASSA_SHOP_ID` для платёжного шлюза ЮKassa;
  - `AUTH_SERVICE_GRPC_URL` для подключения к auth-manager.

=== Сценарии использования

*Организатор (через REST API events-service)*:
- аутентифицируется с помощью JWT-токена от auth-manager;
- создаёт новое мероприятие, указывая название, описание, дату, место;
- настраивает волны регистрации и билетные категории;
- определяет промокоды с процентными скидками;
- просматривает список зарегистрировавшихся участников с фильтрацией по волне и статусу;
- экспортирует список гостей для печати.

*Участник (через REST API events-service)*:
- просматривает доступные мероприятия и волны регистрации;
- регистрируется, заполняя форму (ФИО, email, Telegram);
- применяет промокод и видит финальную цену;
- переходит по платёжной ссылке, созданной payment-service;
- получает QR-код билета через Telegram от notifier;
- использует QR-код для входа на мероприятие.

*Система (асинхронная обработка через Kafka)*:
- events-service публикует событие о новой регистрации в топик `registrations`;
- payment-service создаёт платёжную сессию и публикует о ней в топик `payments`;
- payment-service получает вебхук от ЮKassa и обновляет статус в БД и публикует в Kafka;
- qr-service потребляет события из топика `qr-generation`, генерирует QR-код и загружает в MinIO;
- события о завершении публикуются в топик `ticket-status-updates` для уведомления других сервисов.

=== Обслуживание и поддержка

+ *Резервное копирование*: ежедневное резервное копирование БД PostgreSQL в архив (локальный диск или S3);

+ *Мониторинг*: сбор метрик через Prometheus с интервалом 30 секунд; визуализация в Grafana; оповещение об аномалиях;

+ *Логирование*: логи всех микросервисов сохраняются в папке `deploy/logs/` с ротацией по размеру (до 1 ГБ per file);

+ *Масштабируемость*: система рассчитана на 10--100 организаторов и 100--5 000 участников в зависимости от выделенных ресурсов контейнерам (CPU, память);

+ *Обновление версии*: миграция данных БД через `sqlc` и ручные скрипты миграции; откат возможен сохранением резервных копий перед обновлением.
